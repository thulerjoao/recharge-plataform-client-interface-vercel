import {
  colors,
  bigoColors,
  lavixColors,
  zeColors,
  resellerColors,
} from "./modules/color";

export const Theme = {
  colors,
  bigoColors,
  lavixColors,
  zeColors,
  resellerColors,
};
